/**
 * Next config — deliberately almost empty.
 *
 * Vercel needs `web/` set as the project Root Directory (docs/SETUP.md); everything else is
 * Next's default on purpose. Add a setting here only with a comment saying what broke without it.
 */
/** @type {import('next').NextConfig} */
export default {};
