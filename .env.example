# Copy to .env.local and fill in. NEVER commit the real file.
# Safe to expose (ships in the browser bundle; protected by RLS):
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# NEVER put SUPABASE_SERVICE_ROLE_KEY here or anywhere in this repo.
# It lives only in GitHub Actions secrets and the Supabase dashboard.
